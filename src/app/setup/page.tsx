import { SetupForm } from "@/components/setup-form";

export default function SetupPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-solva-soft p-4">
      <SetupForm />
    </main>
  );
}
