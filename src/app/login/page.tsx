import { LoginForm } from "@/components/login-form";

export default function LoginPage() {
  return (
    <main className="auth-surface flex min-h-screen items-center justify-center p-4">
      <LoginForm />
    </main>
  );
}
